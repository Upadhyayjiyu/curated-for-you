// Example JS: Show a small alert when clicking buttons
document.querySelectorAll(".btn").forEach(button => {
  button.addEventListener("click", () => {
    alert("Thanks for exploring your curated recommendations! 🚀");
  });
});
